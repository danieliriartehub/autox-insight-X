# ── Configuración de Bun ───────────────────────────────────────────────────────
# Escudo de seguridad supply-chain: salta versiones publicadas hace < 24h.
# Excluye paquetes específicos del guard si es necesario.

[install]
minimumReleaseAge = 86400
minimumReleaseAgeExcludes = ["@lovable.dev/vite-tanstack-config"]
